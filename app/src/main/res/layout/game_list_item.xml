<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/mainItemLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/gameFrontCover"
        android:layout_width="80dp"
        android:layout_height="115dp"
        android:src="@drawable/default_front_cover" />

    <TextView
        android:id="@+id/gameTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginLeft="8dp"
        android:layout_marginStart="8dp"
        android:layout_toEndOf="@+id/gameFrontCover"
        android:layout_toLeftOf="@+id/favourite"
        android:layout_toRightOf="@+id/gameFrontCover"
        android:layout_toStartOf="@+id/favourite"
        android:gravity="center"
        android:textColor="@color/colorAccentJP"
        android:textSize="16sp" />

    <ImageView
        android:id="@+id/favourite"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:src="@drawable/normal" />
</RelativeLayout>